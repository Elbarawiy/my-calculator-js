function appendTodisplay(value){
    document.getelentById("display").value= value;
}

function clearDisplay(){
    document.getElementById("display"). value = "";
    }
    function calculate(){
       try {
        const result=eval(document.getElementById("display").value = result;

        }catch (e) {
            alert( "invalid expression");
        }
        
    }

